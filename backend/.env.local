# This is a .env file template

DO_AGENT_URL="your-agent-url"
DO_AGENT_KEY="your-agent-api-key"

FLASK_ENV=development # Update to "production" in prod env
DATABASE_URL=sqlite:///utp_chatbot.db # Update to your production database URL if not using SQLite
